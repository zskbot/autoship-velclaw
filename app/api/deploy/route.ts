import { NextResponse } from "next/server";import { deployGitHubRepository } from "@/lib/vercel";export const runtime="nodejs";export async function POST(request:Request){try{const b=await request.json();const repo=typeof b?.repo==="string"?b.repo.trim():"";const branch=typeof b?.branch==="string"?b.branch.trim():"main";if(!repo)return NextResponse.json({error:"Repository URL is required."},{status:400});if(!branch)return NextResponse.json({error:"Branch is required."},{status:400});return NextResponse.json(await deployGitHubRepository({repoUrl:repo,branch}))}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Deployment failed."},{status:500})}}
